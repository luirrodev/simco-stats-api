# Database credentials
DB_USER="postgres"
DB_PASS="s3cr3t_p4ssw0rd"
DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="simco_dev_db"

# Constructed database URL
DATABASE_URL="postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# Game credentials
GAME_EMAIL="user@example.com"
GAME_PASSWORD="ChangeMe!"

# Other settings
TIMEZONE_OFFSET=-180 # Example: UTC-3 in minutes